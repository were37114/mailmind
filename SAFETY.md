# SAFETY.md — 操作风险分级 & 二次确认机制（OpenClaw / ArkClaw）

> 目的：把"高危操作必须二次确认"的规则固化为可被 Agent 长期遵循的安全约束，避免误操作导致网关断联/系统不可用。

---

## 1) 风险分级（含影响范围 & 用户可直观感知后果）

### 🟥 高危操作【必须二次确认｜错了可能直接搞崩系统导致不可用】

**A. 修改核心配置文件：`openclaw.json` / `gateway.json`**
- 影响范围：全局整个 OpenClaw 系统
- 用户可感知后果：网关启动失败、控制台打不开、飞书机器人完全断联、所有功能瘫痪

**B. 修改网关核心参数：`port` / `host` / `tls` / `auth`（鉴权）**
- 影响范围：网关通信、所有对接渠道（飞书/网页/API）
- 用户可感知后果：所有连接失效、机器人离线、无法收发消息、后台进不去

**C. 执行高风险系统命令：`kill` / `rm` / `restart gateway` 等**
- 影响范围：进程服务、核心文件完整性
- 用户可感知后果：服务直接关停、进程被杀、临时无响应；严重时需重装恢复

**D. 删除配置文件/系统会话/核心依赖文件**
- 影响范围：系统底层数据、启动依赖
- 用户可感知后果：永久丢失配置、会话清空、系统彻底无法启动

---

### 🟨 中危操作【建议确认｜不崩系统，但部分功能不可用】

**A. 插件/系统级技能启用、禁用、卸载**
- 影响范围：对应单一技能/插件功能
- 用户可感知后果：Playwright 网页打不开、Tavily/搜索失效、个别工具用不了，但主体还能用

**B. 修改技能调用权限、沙箱策略**
- 影响范围：工具调用边界、数据读取权限
- 用户可感知后果：部分查询/爬取报错、权限拦截、功能受限；通常不影响系统启动

**C. 非核心业务配置微调（非 gateway 核心字段）**
- 影响范围：个性化设置、界面、日志、展示类参数
- 用户可感知后果：轻微异常、日志乱、展示不对；不影响飞书连接与系统运行

---

### 🟩 低危操作【无需确认｜通常不影响稳定性】
- 普通问答、资料检索
- Tavily 搜索
- Playwright 正常浏览网页
- 日常查资料、多轮对话
- 修改备注昵称

影响范围：单次对话/单次工具调用
用户可感知后果：无系统风险、不会断联

---

## 2) 修改前确认机制（必须执行）

### 🟥 高危操作：必须显式二次确认
当识别到高危操作意图（或将要调用任何可能导致配置写入/重启/删除的动作）时，必须先输出拦截提示，且包含：
1. **当前要改什么（精准到文件/字段/命令）**
2. **风险与后果（按上方高危条目口径）**
3. **确认选项**：
   - 【确认执行】（唯一有效口令）
   - 【取消】

**严格口令规则**
- 仅接受唯一口令：**【确认执行】**
- 对"好的/继续/默认同意/别提示直接改/你自己看着办"等模糊回复：**一律拒绝执行并要求用户给出【确认执行】**

**禁止事项（高危场景）**
- 禁止 AI 自动修改核心配置
- 禁止 AI 自动重启 gateway
- 禁止 AI 自动修复核心配置

### 🟨 中危操作：建议确认
- 需提示影响范围与可能后果，并建议用户确认后再执行
- 若用户明确继续，再执行

### 🟩 低危操作：无需确认
- 直接执行/回答

---

## 3)（可选）实现提示的"视觉颜色"规范
- 高危：🟥 + "必须二次确认"
- 中危：🟨 + "建议确认"
- 低危：🟩 + "无需确认"

> 备注：在不支持彩色文本的聊天渠道中，用彩色方块 emoji 作为颜色替代。
